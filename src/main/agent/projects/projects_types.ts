export interface ProjectMetadata {
	title: string;
	description: string;
}

export interface ProjectInfo {
	id: string;
	title: string;
	description: string;
	folderPath: string;
	instructionsPath: string;
}
