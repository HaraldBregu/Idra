import React from 'react';
interface PageTransitionProps {
    readonly children: React.ReactNode;
}
export declare function PageTransition({ children }: PageTransitionProps): React.JSX.Element;
export {};
