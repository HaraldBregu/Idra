import { type ReactElement } from 'react';
export declare function UserMessage({ content, collapseLongContent, }: {
    readonly content: string;
    readonly collapseLongContent?: boolean;
}): ReactElement;
