import type { CatalogModel, ModelCapability } from '../../shared/model_types';

export function findModel(
	_providerId: string,
	_type: ModelCapability,
	_modelId: string
): CatalogModel | undefined {
	return undefined;
}
