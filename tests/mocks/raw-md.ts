/**
 * Stub for `*.md?raw` imports (Vite raw-string loader).
 * Returns an empty string so any module that reads bundled markdown
 * content doesn't crash in the Jest environment.
 */
export default '';
