# Examples

Working apps built on [`@friday/sdk`](../sdk).

| Example                 | Shows                                                                                                                                                                      |
| ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [projects](./projects/) | React widget over the agent's project tools — streaming run state, structured tool results, tool permission prompts, and both SDK modes (preload globals and `connect()`). |

Build the SDK before installing an example: `npm --prefix ../sdk run build`.
